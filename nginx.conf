server {

    root /static;
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name test.gresse.io;
    
    index index.html;

    location = / {
        index indexLanding.html
    }

}
